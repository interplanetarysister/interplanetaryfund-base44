# ✨🌟 Base44 Snapshot — ARCHIVAL REFERENCE 🌟✨

> **✨ ARCHIVAL CANDIDATE — HISTORICAL BASE44 SNAPSHOT. ✨**
>
> Do not start new production work here. Preserve this repository only while unique Base44 functionality and deployment dependencies are being reconciled into the canonical Interplanetary Fund repositories.

## Canonical work destinations

- User-facing application work → `interplanetarysister/InterplanetaryFund`
- Primary application/integration work → `interplanetarysister/interplanetary-fund2`
- Backend, Convex, security, treasury and operations → `interplanetarysister/interplanetary-fund-backend`

## Historical Base44 usage

This repository was used to run and edit the app locally and publish changes back through Base44. It is retained as a migration/reference snapshot until its remaining unique functionality and deployment connections are verified.

## Prerequisites

1. Clone the repository using the project's Git URL.
2. Navigate to the project directory.
3. Install dependencies: `npm install`.
4. Install the Base44 CLI: `npm install -g base44@latest`.

## Run Locally

```bash
base44 dev
```

For frontend-only development against the hosted backend:

```bash
npm run dev
```

## Migration safety

- Do not make this repository a competing production source of truth.
- Do not copy credentials or secrets during consolidation.
- Preserve unique behavior until it is deliberately migrated or retired.
- Verify Base44 publishing/deployment dependencies before final archival.
